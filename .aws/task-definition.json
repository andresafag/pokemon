{
  "family": "pokemon",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::AWS_ACCOUNT_ID:role/pokemon-ecs-execution-role",
  "taskRoleArn": "arn:aws:iam::AWS_ACCOUNT_ID:role/pokemon-ecs-task-role",
  "containerDefinitions": [
    {
      "name": "pokemon",
      "image": "IMAGE_URI_PLACEHOLDER",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 10000,
          "hostPort": 10000,
          "protocol": "tcp"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/pokemon",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "environment": [],
      "secrets": []
    }
  ]
}
